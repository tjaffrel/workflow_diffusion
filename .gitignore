**/.DS_Store
**/*.res
**/*.volpo
**/*.cif
test_cifs
**/__pycache__
# *.ipynb
*.stdout
*.stderr
workflow_output

# Data output (top-level only — not mofgen_tools/data/)
/data/*
# ...but keep the small, versioned figure source data
!/data/figure_source_data/

# Environment files with secrets
.env

# pytest cache
.pytest_cache/

# editable install / build artifacts
*.egg-info/
build/
dist/
